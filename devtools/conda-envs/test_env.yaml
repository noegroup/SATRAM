name: test
channels:

  - conda-forge
  - pytorch
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pytorch

    # Pip-only installs
  #- pip:
  #  - codecov

